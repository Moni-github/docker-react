language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t Monica/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Moni-github/docker-react npm run test
